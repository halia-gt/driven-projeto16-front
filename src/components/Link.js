import styled from "styled-components";

export default function Rank({ id, shortUrl, url, visitCount }) {
    return (
        <Li>
            <section>
                <span></span>
                <span></span>
                <span></span>
            </section>
            <div>

            </div>
        </Li>
    );
}

const Li = styled.li`
    margin-bottom: 40px;
    border-radius: 12px;

`;